# Personal data — never commit this. It holds your real tasks, loops,
# calendar hours, and name. Otto creates it fresh on first run.
otto-data.json
otto-data.*.json

# Personal Claude Code context — not project documentation, just notes
# for continuity between chat and the IDE. CLAUDE.md (committed) has the
# shared, public-safe project facts; this has the rest.
CLAUDE.local.md

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Just in case a stray backup/export ever lands here
*.bak
*.local.json
